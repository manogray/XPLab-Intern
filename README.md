# Desafio XPLab Bemol

## Descrição

O desafio foi solucionado usando Python 3 com as bibliotecas pandas e plotly

## Execução

Para a execução dos scripts, tanto o script em python como o notebook do Jupyter, deve ser feito a instalação dos seguintes pacotes

```bash
$ pip install plotly==4.1.0
```

```bash
$ pip install pandas
```

```bash
$ pip install psutil requests
```

```bash
$ npm install -g electron@1.8.4 orca
```

